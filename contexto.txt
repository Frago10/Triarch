################################################################################
#  TRIARCH BOT — CONTEXTO DEL PROYECTO
#  Última actualización: 2026-05-12
#
#  PROPÓSITO DE ESTE ARCHIVO
#  -------------------------
#  Memoria viva del proyecto. Se lee PRIMERO al retomar el trabajo en una sesión
#  nueva. Cuando Claude empieza desde cero, lee esto y reconstruye el estado
#  mental del proyecto sin tener que re-explorar todo el código.
#
#  CÓMO ACTUALIZARLO
#  -----------------
#  Al final de cada sesión, actualizar como mínimo:
#    · §03  ETAPA ACTUAL
#    · §10  CHANGELOG
#    · §11  PRÓXIMOS PASOS
#  El resto solo si hubo cambio estructural.
#
#  ÍNDICE
#  ------
#  §01  Resumen ejecutivo del proyecto
#  §02  Filosofía y reglas inquebrantables
#  §03  ETAPA ACTUAL — dónde estamos ahora mismo
#  §04  Arquitectura — mapa de carpetas
#  §05  Perfiles por activo
#  §06  Switches en vivo (runtime.yaml)
#  §07  Dashboard — pestañas y contratos
#  §08  Backtesting — cómo se corre y qué reporta
#  §09  Comandos de uso (PowerShell)
#  §10  Changelog (inverso)
#  §11  Próximos pasos
#  §12  Notas técnicas y lecciones
#  §13  Front-end "Crimson Black" (UI v4) — 2026-05-20
#  §14  Front-end estático para GitHub Pages — 2026-05-20
#  §15  Pool ampliado de estrategias + filtros temporales (v0.5) — 2026-05-20
#  §16  Backtester en JavaScript (v0.6, corre en browser) — 2026-05-20
#  §17  Calibración v0.7 sobre data MT5 real — 2026-05-26
################################################################################


################################################################################
#  §01  RESUMEN EJECUTIVO
################################################################################

Triarch es un bot de trading sistemático sobre MetaTrader 5.

  • Multi-activo:    NAS100 (índice), XAUUSD (commodity), EURUSD (FX)
  • Multi-estrategia con capa de confluencia entre familias
  • 3 modos de ejecución por activo: SIGNAL_ONLY | APPROVAL | AUTO
  • 3 capas de aprendizaje (planificadas):
        v1  Postmortem LLM en Obsidian (implementado)
        v2  ML clásico XGBoost (pendiente)
        v4+ RL sobre el ejecutor (lejano)

Codename "Triarch" = 3 activos × 3 modos × 3 capas de aprendizaje.


################################################################################
#  §02  FILOSOFÍA — REGLAS INQUEBRANTABLES
################################################################################

Heredadas de Roybot:

  1. Multi-estrategia con confluencia > una sola estrategia mágica
  2. Risk manager con caps duros (SL obligatorio, RR mínimo, daily loss cap)
  3. Audit trail forense — cada decisión queda registrada, incluyendo rechazos
  4. Empezar simple, iterar (no añadir estrategia #5 sin promocionar las 4)
  5. No saltar de fase sin métricas pre-definidas que se cumplan

Reglas que NUNCA se eliminan sin nota en la bitácora del proyecto:

  · SL siempre presente al colocar una orden
  · RR mínimo respetado (varía por activo: 0.8 EURUSD, 1.5 NAS100, 2.2 XAUUSD)
  · Daily loss cap activo
  · Max trades por día por activo
  · Kill switch global (TRIARCH_KILL=1)


################################################################################
#  §03  ETAPA ACTUAL
################################################################################

Fecha: 2026-05-14

ESTAMOS EN: 3er activo cambiado a EURUSD + arreglado el bug de descarga de
histórico que hacía que los pares en M5 (forex) devolvieran 0 velas.

──────────────────────────────────────────────────────────────────────────
FIX: DESCARGA DE HISTÓRICO EN M5 (forex devolvía 0 velas)  (2026-05-14)
──────────────────────────────────────────────────────────────────────────
SÍNTOMA: USDJPY y luego EURUSD no devolvían data en el backtest, pero
NAS100 (M15) y XAUUSD (M30) sí. El usuario lo notó: "raro que el par no
muestre data".

CAUSA RAÍZ: data_layer/mt5_client.get_rates hacía UNA sola llamada gigante a
`mt5.copy_rates_range(symbol, tf, from_date, now)`. MT5 solo devuelve lo que
está en la caché local del terminal; para M5 a 1 año (~75k velas) esa caché
casi nunca cubre todo → copy_rates_range devuelve VACÍO (no parcial, vacío).
Con M15/M30 el rango es chico (~17k / ~8k velas) y sí está en caché.
NO era un problema de "símbolo inexistente" ni de que fuera forex — era
M5 + rango largo en una sola llamada.

FIX APLICADO en data_layer/mt5_client.py:
  · get_rates ahora descarga EN CHUNKS (~5000 velas por chunk: M5≈17 días,
    M15≈52d, M30≈104d) y concatena.
  · Cada chunk se pide con reintentos (_rates_range_retry): MT5 baja el
    histórico async, así que la 1ª llamada puede venir vacía y disparar la
    descarga; las siguientes ya traen datos.
  · Modo "live" (from_date=None) usa _rates_pos_retry, también con reintentos.
  · Si TODOS los chunks vienen vacíos → mensaje claro: o el símbolo no existe,
    o hay que abrir el gráfico en MT5 y hacer scroll atrás para que baje
    history, y re-correr (fetch_history es incremental).
  · fetch_history.py: mensajes de error actualizados (distingue "símbolo mal"
    de "history no bajado").

──────────────────────────────────────────────────────────────────────────
CAMBIO DE ACTIVO: USDJPY → EURUSD  (2026-05-14)
──────────────────────────────────────────────────────────────────────────
El 3er activo pasó de USDJPY a EURUSD en TODO el proyecto (symbols.yaml,
runtime.yaml, README, docs, comentarios, etc.). Razón: decisión del usuario.
EURUSD conserva el perfil "scalper puro": SCALPER + BB_MR, confluencia 1/1,
min_rr 0.8, cap 8 trades/día, objetivo ~30/semana.
Diferencia vs USDJPY: la sesión cambió a 07:00-16:00 UTC (Londres + solape NY,
la franja fuerte de EURUSD).
Verificación: backtest sintético EURUSD → 755 trades, WR 52%, lógica corre OK.

──────────────────────────────────────────────────────────────────────────
RESULTADOS DEL TUNING #2 (2026-05-14)
──────────────────────────────────────────────────────────────────────────

XAUUSD M30 — backtest sobre 1 año de DATA REAL:
  Probé cada estrategia sola y combinada. Hallazgo:
    · ORB sola          → PIERDE (PF 0.92). Demasiados falsos breakouts.
    · BB_MR sola        → marginal (PF 1.10).
    · EMA_MOMENTUM sola → PF 1.51 · WR 41% · E +0.30R · DD 7R · 2.6 tr/sem  ★
    · Combinarlas BAJA el PF (confluencia a veces elige la peor señal).
  CONFIG ELEGIDA: EMA_MOMENTUM sola, min_rr 2.2, confluencia 1/1.
  → trades=105 · WR 40.9% · PF 1.51 · E +0.303R · avgW 2.18R · DD 7R

EURUSD M5 — NO HAY DATA REAL TODAVÍA. Backtest sobre M5 SINTÉTICO:
  El SCALPER produce trades correctamente: 755 trades, WR 52%, ~15 tr/sem.
  En data aleatoria da ~breakeven (esperado). La lógica funciona; falta
  validar y tunear sobre DATA REAL de EURUSD.

──────────────────────────────────────────────────────────────────────────
⚠  LA VERDAD SOBRE "50-60% WR + RR 1:2-1:3" (decisión importante)
──────────────────────────────────────────────────────────────────────────
Esa combinación NO existe en trading sistemático real: implicaría un Profit
Factor de 2.5 a 4.5. Hay un tradeoff físico:
   · Estrategias de momentum/breakout → WR 35-45%, wins grandes (RR 2-3)
   · Estrategias de mean-reversion    → WR 55-65%, wins chicos  (RR 0.6-1)
EMA_MOMENTUM en oro da 41% WR con RR 2.2 → PF 1.51. Es un sistema SÓLIDO.
Para tener 55%+ WR habría que aceptar RR ~1:1 (cambiar a mean-reversion puro).
Recomendación registrada: quedarse con el perfil momentum en oro; el WR de
41% con PF 1.51 es mejor negocio que 55% WR con PF 1.1.
Lo mismo aplica al scalper de EURUSD: con RR 0.85 necesita WR > ~54% para
ser rentable — alcanzable porque el scalper apuesta a hit-rate, no a RR.

──────────────────────────────────────────────────────────────────────────
EURUSD — QUÉ HACER PARA TENERLO OPERATIVO
──────────────────────────────────────────────────────────────────────────
EURUSD existe en prácticamente todo broker con el nombre exacto "EURUSD".
ACCIÓN (PASO 1 de §11): bajar data real con fetch_history y backtestear.
Si por algún motivo el broker lo llama distinto, fetch_history ahora avisa
y sugiere el nombre correcto.

──────────────────────────────────────────────────────────────────────────
BUGS DE INFRAESTRUCTURA ENCONTRADOS Y CORREGIDOS
──────────────────────────────────────────────────────────────────────────
  · Estrategias hardcodeaban rr_target=1.5 → ahora RR dinámico desde cfg.
  · Backtester no aplicaba RiskManager → ahora aplica RR-min + cap diario +
    sesión, y reporta señales descartadas por cada causa.
  · Backtester recalculaba indicadores por ventana (lento, ~2min/año) →
    ahora los precalcula 1 vez (~6s/año, 20x más rápido).
  · `future = df_full.iloc[i+1:]` creaba slices gigantes → OOM. Acotado a 250.
  · Confluencia era global → ahora configurable por activo (build_confluence_for).

ARCHIVOS TOCADOS EN ESTA SESIÓN:
  · strategies/orb.py, ema_momentum.py, bb_mr.py — RR dinámico desde cfg
  · strategies/bb_mr.py                  — NUEVA (Bollinger Mean Reversion)
  · strategies/scalper.py                — 2 gatillos + filtro ema_sep_min
  · strategies/registry.py               — registra BB_MR
  · scripts/backtest.py                  — risk filters + precompute + slice OOM fix
  · scripts/fetch_history.py             — sugiere símbolos cuando uno falla
  · config/settings.py                   — ConfluenceOverride en SymbolConfig
  · config/symbols.yaml                  — XAUUSD=EMA_MOMENTUM@2.2 ·
                                           EURUSD=SCALPER+BB_MR · confluencia x activo
  · confluence/filter.py                 — build_confluence_for(symbol, settings)
  · engine/orchestrator.py               — confluencia por activo
  · dashboard/app.py                     — usa backtest_symbol nueva firma

LO QUE SIGUE INMEDIATAMENTE: ver §11.


################################################################################
#  §04  ARQUITECTURA — MAPA DE CARPETAS
################################################################################

Raíz del proyecto:
    C:\Users\jeanp\OneDrive\מסמכים\OB vault\03 - Resources\Data Engineering\Python\triarch

    config/           settings + symbols.yaml + runtime.yaml (overrides live)
                      └─ runtime.py: API para leer/escribir overrides
    data_layer/       MT5Client (wrapper oficial MetaTrader5)
    engine/           indicadores + orchestrator (loop principal)
    strategies/       ORB, VWAP_MR, EMA_MOMENTUM, SCALPER, BB_MR
                      └─ registry.py mapea nombre → clase
    confluence/       filtro mín-señales + mín-familias
    risk/             RiskManager (caps, kill-switch, lock-outs)
    executor/         SIGNAL_ONLY | APPROVAL | AUTO + sizing + trade_monitor
    signals/          schema Pydantic + notifiers (Telegram, Discord)
    audit/            store SQLite + writer Obsidian
    learning/         postmortem semanal (v1) — features ML pendiente
    dashboard/        Streamlit (6 pestañas, ver §07)
    scripts/          entrypoints CLI:
                        connect_mt5, run_live, serve, backtest, fetch_history,
                        db_summary, diagnose_mt5
    tests/            pytest (indicators, orb, risk, confluence, smoke)
    docs/             setup-mt5-demo, architecture
    data_cache/       triarch.sqlite (audit) + history/*.parquet (velas)
    logs/             rotación de loguru


################################################################################
#  §05  PERFILES POR ACTIVO
################################################################################

NAS100   — perfil "balanced"
    TF M15  ·  estrategias: ORB + VWAP_MR + EMA_MOMENTUM
    Confluencia: 2 señales / 2 familias / score ≥ 1.0
    Objetivo: ~8 trades/semana
    Sesión: 13:30–17:00 UTC (cash open NY)
    Risk: 0.5% por trade  ·  daily cap 2%  ·  máx 5 trades/día  ·  RR ≥ 1.5

XAUUSD   — perfil "quality"  (config validada en backtest 2026-05-14)
    TF M30 (menos ruido que M15)
    Estrategias: EMA_MOMENTUM SOLA — es la única rentable en oro.
      Backtest: ORB sola pierde (PF 0.92), BB_MR marginal (1.10), combinarlas
      baja el PF. EMA_MOMENTUM sola = PF 1.51. ORB/BB_MR/VWAP_MR quedan
      disponibles en el registry pero NO en la lista activa de XAUUSD.
    Confluencia: 1 señal / 1 familia (solo hay 1 estrategia activa)
    min_rr_ratio 2.2 → TP1 a 2.2R (RR dinámico)
    Backtest 1 año: 105 trades · WR 41% · PF 1.51 · E +0.30R · DD 7R · 2.6 tr/sem
    Sesión: 12:00–17:00 UTC (solape Londres + NY)
    Risk: 0.75% por trade  ·  daily cap 1.5%  ·  máx 2 trades/día
    NOTA: el WR de 41% es el techo de las estrategias de momentum. 50-60% WR
    + RR 1:2-1:3 a la vez no existe (ver §03). PF 1.51 es un sistema sólido.

EURUSD   — perfil "scalper puro"  (lógica validada, falta data real)
    TF M5  ·  el par más líquido del mundo → spread bajo, ideal para scalping
    Estrategias: SCALPER + BB_MR
    Confluencia PERMISIVA: 1 señal / 1 familia (un scalper no espera a que
    2 estrategias coincidan en la misma vela)
    min_rr_ratio 0.8 → SCALPER opera con RR ~0.85 (TP cercano, WR alto compensa)
    SCALPER tiene filtro ema_sep_min (0.0002, suave) — subirlo tras ver data real
    Objetivo: ~30 trades/semana, rentable a escala
    Sesión: 07:00–16:00 UTC (apertura Londres + solape NY, la franja fuerte)
    Risk: 0.25% por trade (más chico, más volumen) · daily cap 1.5% · máx 8/día
    Backtest SINTÉTICO: 755 trades · WR 52% · ~15 tr/sem · breakeven
      (esperado en data aleatoria — falta data REAL para validar de verdad)
    broker_symbol "EURUSD" casi siempre es el nombre exacto en todo broker


################################################################################
#  §06  SWITCHES EN VIVO  (config/runtime.yaml)
################################################################################

Archivo: config/runtime.yaml
Forma:
    take_trades:
      NAS100: false
      XAUUSD: false
      EURUSD: false

Semántica:
    take_trades = ON   → orchestrator respeta cfg.mode del yaml (puede ejecutar)
    take_trades = OFF  → orchestrator fuerza SIGNAL_ONLY (solo notifica)

El dashboard tab "Live & Control" tiene un toggle por activo que escribe a
este archivo. Cambia efectivo en el próximo tick (no requiere reiniciar).

Para parar TODO de golpe (no por activo): TRIARCH_KILL=1 en .env + reinicio.


################################################################################
#  §07  DASHBOARD — UI v3 (rediseño visual)
################################################################################

Streamlit con paleta dark moderna (cyan/violet accents, glassmorphism, pills
translúcidas con borde, hover en cards).

ARRANQUE:
  Splash de bienvenida (1 vez por sesión, gated por st.session_state) con
  resumen del proyecto y botón "Entrar al bot". Se omite en visitas siguientes.

SIDEBAR (siempre visible):
  · Wordmark TRIARCH con gradient cyan→violet
  · Bloque "Entorno" con dot de color (DEMO=verde, LIVE=naranja)
  · Bloque "Cuenta MT5": login + server + equity + P/L flotante (color)
  · Bloque "Kill switch global": estado + cómo activarlo
  · Atajos: comandos CLI más usados
  · Footer: versión

TABS TOP-LEVEL (5):
  1. 🏠 Inicio — resumen ejecutivo del día
       · 4 KPIs de cuenta (balance, equity con delta, margen libre, apalanc.)
       · Actividad 24h: señales totales, tomadas, rechazadas, cerradas, P/L
       · Mini-card por activo con dot de estado + última señal

  2. 🎯 Vivo & Control
       · Bloque cuenta MT5 con 5 métricas
       · Una tarjeta por activo: perfil, TF, sesión, toggle take_trades,
         última señal con fecha legible + status traducido + motivo

  3. 🧠 Decisiones
       · Vista legible del audit trail con filtros (activo/estrategia/estado/
         dirección/últimos N días)
       · KPI grid arriba (encontradas, tomadas, rechazadas, cerradas)
       · Tabla con columnas amigables; descarga .txt

  4. 📊 Backtesting
       · Form: activos múltiples + rango fechas + botón "Correr"
       · Tabla comparativa con WR/PF/Sharpe/Sortino/SQN/DD/trades-sem
       · Expander por activo con KPI grid (12 métricas), equity curve,
         breakdown por estrategia, trade log
       · Download resumen .txt

  5. 📦 Datos — sub-tabs (vista cruda para debug/export):
       · 📈 Signals — tabla cruda
       · 🔍 Evals   — audit trail crudo
       · 📦 Stats   — agregados últimos 30 días

PALETA Y COMPONENTES:
  · Acento principal: cyan #5eead4 (vars CSS --tri-accent)
  · Acento 2:        violeta #a78bfa (gradients, wordmark)
  · Cards:           glassmorphism rgba(38,39,48,0.55) + blur(4px)
  · Status pills:    translúcidas con borde sutil del mismo tono
  · Dots:            con halo box-shadow del color (verde/rojo/naranja/gris)
  · Hover:           border-color animado + translateY(-2px) en KPIs
  · Empty states:    centrado, emoji, mensaje útil

DEPENDENCIAS:
  · MT5Client: el dashboard intenta conectarse para mostrar cuenta. Si falla,
    muestra warning. Cuando se usa `scripts.serve`, el dashboard corre en un
    subproceso independiente del loop, así que tiene su propia conexión MT5.


################################################################################
#  §08  BACKTESTING
################################################################################

ARCHIVOS:
    scripts/fetch_history.py  → descarga velas MT5 → parquet (data_cache/history/)
    scripts/backtest.py       → corre estrategias sobre los parquet

API REUTILIZABLE (importada por el dashboard):
    from scripts.backtest import backtest_symbol, _format_summary

    res = backtest_symbol(cfg, confluence, from_date=..., to_date=...)
    # res es dict con:
    #   trades, wins, losses, win_rate, profit_factor, expectancy_r,
    #   avg_win_r, avg_loss_r, largest_win_r, largest_loss_r,
    #   max_drawdown_r, longest_win_streak, longest_loss_streak,
    #   avg_bars_held, trades_per_week_avg,
    #   sharpe_ratio, sortino_ratio, sqn,
    #   by_strategy, trade_log, equity_curve, range

LO QUE DEBE PASAR ANTES DEL PRIMER BACKTEST:
    1. .env completo con credenciales MT5
    2. Terminal MT5 abierto
    3. `python -m scripts.fetch_history --years 1`   ← baja parquet
    4. Recién entonces sirve `python -m scripts.backtest` o el tab Backtest

INTERPRETACIÓN DE MÉTRICAS:
    Sharpe ratio: > 1 aceptable · > 2 bueno · > 3 excepcional (anualizado, ojo)
    Sortino     : igual que Sharpe pero solo cuenta volatilidad negativa
    SQN (Van Tharp): > 1.7 sistema decente · > 2.5 bueno · > 3 excelente
    Profit factor: > 1.3 mínimo · > 1.7 deseable · > 2 excelente
    Expectancy en R: PnL medio esperado por trade en múltiplos de R (riesgo)
                     Sistema viable necesita expectancy > 0
    Max drawdown en R: cuántos "trades perfectos perdidos" peor caso


################################################################################
#  §09  COMANDOS DE USO (PowerShell)
################################################################################

# ─── Setup inicial (solo una vez) ───
cd "C:\Users\jeanp\OneDrive\מסמכים\OB vault\03 - Resources\Data Engineering\Python\triarch"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e .
copy .env.example .env
# editar .env con credenciales MT5

# ─── Cada sesión ───
.\.venv\Scripts\Activate.ps1

# Test de conexión MT5
python -m scripts.connect_mt5

# RECOMENDADO: loop + dashboard en un solo comando
python -m scripts.serve --tick 30 --port 8765
# abre http://localhost:8765

# Por separado
python -m scripts.run_live --tick 30
streamlit run dashboard/app.py

# Bajar histórico (necesario antes del primer backtest)
python -m scripts.fetch_history --years 1
python -m scripts.fetch_history --symbol EURUSD --timeframe M5 --years 2

# Backtest desde CLI
python -m scripts.backtest
python -m scripts.backtest --symbol XAUUSD --from 2024-01-01 --to 2025-06-30
python -m scripts.backtest --symbol EURUSD --out logs/bt_eurusd.txt

# Postmortem manual
python -m learning.postmortem --week 2026-W19

# Tests unitarios
pytest


################################################################################
#  §10  CHANGELOG (inverso)
################################################################################

[2026-05-14]  Dashboard UI v3 — splash + sidebar + tabs reorganizadas
    Rediseño visual del dashboard sin perder funcionalidad:
    • Splash de bienvenida con wordmark TRIARCH (gradient cyan→violet) y
      tarjeta de 4 puntos. Gated por st.session_state["intro_done"].
    • Sidebar nuevo: branding + entorno + cuenta MT5 (equity, P/L flotante) +
      kill switch + atajos CLI + versión.
    • Header con banner gradient sutil.
    • Tabs reorganizadas: 6 → 5 top-level. Nueva pestaña "🏠 Inicio" con
      resumen del día (KPIs cuenta + actividad 24h + mini-cards por activo).
      Signals/Evals/Stats consolidados en "📦 Datos" como sub-tabs.
    • CSS modernizado: glassmorphism (rgba bg + backdrop-filter), pills
      translúcidas con borde, hover en cards/KPIs, dots con halo,
      empty states con emoji.
    • Variables CSS centralizadas (--tri-accent, --tri-card-bg, etc.).

[2026-05-14]  Fix — descarga de histórico en M5 (forex devolvía 0 velas)
    • data_layer/mt5_client.py — get_rates ahora descarga EN CHUNKS con
      reintentos (antes: 1 sola llamada a copy_rates_range que fallaba en
      M5 + rangos largos). +helpers _rates_range_retry / _rates_pos_retry.
      +constante _BARS_PER_DAY para dimensionar los chunks.
    • scripts/fetch_history.py — mensajes de error que distinguen "símbolo
      incorrecto" de "histórico no bajado en el terminal".
    Causa raíz: copy_rates_range solo devuelve lo cacheado por el terminal;
    M5 a 1 año (~75k velas) no entra → vacío. M15/M30 sí entraban.

[2026-05-14]  Cambio de 3er activo: USDJPY → EURUSD
    Decisión del usuario. Reemplazo en TODO el proyecto:
    • config/symbols.yaml — bloque USDJPY → EURUSD, sesión 07:00-16:00 UTC
                            (franja fuerte de EURUSD). Mismo perfil scalper:
                            SCALPER+BB_MR, confluencia 1/1, min_rr 0.8, cap 8/día.
    • config/runtime.yaml, settings.py, signals/schema.py — refs actualizadas
    • strategies/scalper.py — comentarios → EURUSD
    • scripts/backtest.py, fetch_history.py, diagnose_mt5.py — ejemplos → EURUSD
    • README.md, docs/setup-mt5-demo.md — refs → EURUSD
    Verificación: backtest sintético EURUSD → 755 trades, WR 52%, lógica OK.
    Nota histórica: USDJPY sí existía en el broker, pero el usuario prefirió
    EURUSD (más líquido, spread más bajo, mejor para scalping).

[2026-05-14]  Tuning #2 — backtests corridos por Claude, config validada
    Backtests sobre data real (XAUUSD) + sintética (USDJPY):
    • config/symbols.yaml — XAUUSD ahora EMA_MOMENTUM SOLA @ min_rr 2.2,
                            confluencia 1/1 (ORB perdía, combinar bajaba PF).
                            USDJPY: cap diario 12→8, comentarios de diagnóstico.
    • strategies/scalper.py — +filtro ema_sep_min (0.0002, suave) + flag
                            pullback_only. Filtra rangos muertos.
    • scripts/backtest.py — precalcula indicadores 1 vez (20x más rápido);
                            `future` slice acotado a 250 velas (arreglo OOM).
    RESULTADOS: XAUUSD 1 año → PF 1.51, WR 41%, E +0.30R, DD 7R.
                USDJPY sintético → WR 53.7%, ~24 tr/sem (breakeven, data random).

[2026-05-12]  Tuning #1 — RR dinámico, BB_MR, confluencia por activo
    Tras leer el primer backtest (XAUUSD breakeven, USDJPY sin datos):
    • strategies/orb.py, ema_momentum.py — TP1 usa max(rr_propio, cfg.min_rr)
    • strategies/bb_mr.py     — NUEVA: Bollinger Mean Reversion (familia "mean")
    • strategies/scalper.py   — reescrito: 2 gatillos, RR ~0.85, filtros sesión
    • strategies/registry.py  — registra BB_MR
    • scripts/backtest.py     — aplica RR-min + cap diario + sesión; reporta
                                señales descartadas; confluencia por activo
    • scripts/fetch_history.py — sugiere símbolos al fallar uno; resumen OK/fail
    • config/settings.py      — ConfluenceOverride opcional en SymbolConfig
    • config/symbols.yaml     — XAUUSD 4 estrategias + confluencia 2/2 + min_rr
                                2.5; USDJPY SCALPER+BB_MR + confluencia 1/1 +
                                min_rr 0.8
    • confluence/filter.py    — build_confluence_for(symbol, settings)
    • engine/orchestrator.py  — confluencia por activo (dict por símbolo)
    • dashboard/app.py        — backtest_symbol nueva firma (sin param confluence)

[2026-05-12]  Dashboard 2.0 + métricas avanzadas
    • dashboard/app.py    REWRITE — cards, status traducido, fechas legibles,
                          merge Live+Control, nuevo tab Backtesting completo
    • scripts/backtest.py +Sharpe, +Sortino, +SQN, +avg win/loss, +rachas,
                          +equity_curve, +trade_log, +parámetro to_date
    • contexto.txt        Reescrito con índice numerado y secciones marcadas

[2026-05-12]  Reconstrucción tras incidente OneDrive
    • scripts/serve.py            ← entrypoint unificado loop+dashboard
    • scripts/fetch_history.py    ← descarga histórico MT5 → parquet
    • scripts/backtest.py         ← backtester con métricas básicas
    • strategies/scalper.py       ← nueva estrategia SCALPER para USDJPY
    • strategies/registry.py      ← registra SCALPER
    • config/symbols.yaml         ← perfiles quality (XAUUSD) y scalper (USDJPY)
    • config/settings.py          ← SymbolConfig +take_trades/profile/target
    • config/runtime.py           ← nuevo: get/set overrides en vivo
    • config/runtime.yaml         ← nuevo: archivo de overrides persistidos
    • engine/orchestrator.py      ← respeta take_trades runtime
    • dashboard/app.py            ← +Control +Decisiones (versión inicial)

[2026-05-06]  Último snapshot conocido pre-incidente OneDrive
    Arquitectura base: 3 estrategias originales (ORB/VWAP_MR/EMA_MOMENTUM),
    risk manager, audit store, confluence, executor multi-modo, postmortem v1.


################################################################################
#  §11  PRÓXIMOS PASOS
################################################################################

INMEDIATO (la próxima sesión)
    [ ] BAJAR DATA REAL DE EURUSD Y BACKTESTEAR (el bug de M5 ya está arreglado
        — ver §03; ahora descarga en chunks):
          1. `python -m scripts.fetch_history --symbol EURUSD --timeframe M5 --years 1`
             → si avisa "0 velas / histórico no bajado": abre el gráfico de
               EURUSD M5 en MT5, scroll hacia atrás, y RE-CORRE el comando
               (es incremental, cada pasada baja más).
          2. `python -m scripts.backtest --symbol EURUSD --out logs/bt_eurusd.txt`
          3. Mandar el .txt a Claude para tunear el SCALPER con DATA REAL.
    [ ] git_push.ps1 — commitear y pushear esta versión (incluye el fix de
        descarga M5 + el cambio a EURUSD). Repo: https://github.com/Frago10/Triarch
    [ ] RE-CORRER backtest de XAUUSD con la config nueva para confirmar:
            python -m scripts.backtest --symbol XAUUSD --out logs/bt_xau.txt
        Esperado (ya verificado por Claude en sandbox):
          ~105 trades · WR ~41% · PF ~1.51 · E +0.30R · DD ~7R · ~2.6 tr/sem
    [ ] pytest — verificar que orchestrator/settings/registry no se rompieron.

DECISIÓN PENDIENTE DEL USUARIO (sobre el win rate de oro)
    El objetivo "50-60% WR + RR 1:2-1:3" no es alcanzable (ver §03). Opciones:
      A) Quedarse con momentum: 41% WR, PF 1.51 — RECOMENDADO, es sólido.
      B) Cambiar a mean-reversion puro en oro: ~55-60% WR pero RR ~1:1,
         PF más bajo (~1.1-1.2). Más "cómodo" psicológicamente, peor negocio.
    → Claude recomienda A. El usuario decide.

CORTO PLAZO (esta semana) — depende del backtest de EURUSD con data real
    [ ] Tunear SCALPER sobre data REAL de EURUSD:
        · si <30 tr/sem → bajar rel_atr_min (0.0003) o ema_sep_min (0.0002),
          o ampliar la sesión (hoy 07:00-16:00 UTC)
        · si WR <54%   → subir ema_sep_min, o probar pullback_only=True,
          o ajustar tp_atr_mult/sl_atr_mult
    [ ] Probar dashboard "Decisiones" y "Backtesting" — validar visualmente.
    [ ] Probar pausar/reanudar con tab "Live & Control" en sesión real.

MEDIO PLAZO (próximas 2-4 semanas)
    [ ] Empezar learning/features.py — guardar features con cada signal.
    [ ] Telegram con botones inline para APPROVAL mode (build_default_notifiers
        ya tiene scaffold).
    [ ] Reporte semanal automático (cron + postmortem.py).
    [ ] Filtrar duplicados de signals por mismo (symbol, bar_time) más estricto
        — hoy el dedup_key incluye strategy y direction.

NO TOCAR HASTA tener métricas de promoción
    [ ] AUTO mode con dinero real
        Criterios: ≥ 3 meses demo, PF ≥ 1.3, max DD < 10R, win rate > 40%
    [ ] ML filter en producción (≥ 6 meses de signals reales)
    [ ] RL (≥ 1 año real)


################################################################################
#  §12  NOTAS TÉCNICAS Y LECCIONES
################################################################################

ONEDRIVE
    OneDrive movió la carpeta Documents al folder OneDrive sin avisar.
    Resultado: la única copia estable es la de OneDrive.
    Mitigación: inicializar git LOCAL guarda historial independiente de la nube
    y permite rollback.
    OneDrive guarda hasta 25 versiones por archivo accesibles desde web
    (click derecho → Historial de versiones) y mantiene papelera 30 días.

SINCRONIZACIÓN VS BASH
    Cuando se editan archivos por el host, la vista del workspace bash puede
    estar stale temporalmente (OneDrive demora unos segundos). Para validar
    sintaxis fiable, usar la lectura del host (Read tool) o esperar y reintentar.

SCALPER — TUNING (parámetros actuales tras tuning #2)
      rel_atr_min: 0.0003   ·  sl_atr_mult: 1.0  ·  tp_atr_mult: 0.85 → RR ≈ 0.85
      ema_sep_min: 0.0002 (suave) ·  max_dist_ema9_atr: 0.6
      2 gatillos: pull-back (cruce EMA9) + continuación (precio entre EMA9/EMA21)
      flag pullback_only: si True, desactiva el gatillo de continuación
    Si en backtest da pocos trades → bajar rel_atr_min o ema_sep_min.
    Si da muchos con WR bajo → subir ema_sep_min (0.0004-0.0008), o
      pullback_only=True (el pull-back es el gatillo más fiable).
    OJO: tunear SOLO sobre data REAL. En sintético (random walk) el SCALPER
    da ~breakeven por definición; un filtro de tendencia no aporta nada ahí.

XAUUSD — TUNING (config validada en backtest #2)
    EMA_MOMENTUM sola @ min_rr 2.2 → PF 1.51, WR 41%, E +0.30R (1 año real).
    ORB sola pierde plata en oro. Combinar estrategias BAJA el PF (la
    confluencia a veces elige la señal peor). Menos es más en oro.
    El techo de WR de las estrategias de momentum es ~40-44%. Para subir el WR
    habría que cambiar a mean-reversion, sacrificando el RR. Ver §03.

DESCARGA DE HISTÓRICO MT5 — copy_rates_range y el problema de M5
    `mt5.copy_rates_range(symbol, tf, from, to)` NO es fiable en una sola
    llamada para rangos grandes: solo devuelve lo que el terminal tiene en
    caché local. M5 a 1 año ≈ 75k velas → casi nunca está cacheado → devuelve
    VACÍO. M15/M30 a 1 año (~17k / ~8k velas) sí entran, por eso NAS100 y
    XAUUSD bajaban bien y los pares forex en M5 no.
    SOLUCIÓN (ya implementada en get_rates): descargar en chunks chicos
    (~5000 velas c/u) con reintentos. MT5 baja history async — la 1ª llamada
    puede venir vacía y disparar la descarga, las siguientes traen datos.
    Si aun así un símbolo viene vacío: abrir su gráfico en MT5 en ese TF,
    hacer scroll hacia atrás para forzar la descarga, y re-correr fetch_history
    (es incremental). O usar un rango más corto.

BACKTESTING EN SANDBOX (Claude)
    Claude puede correr backtests en su sandbox Linux (no necesita MT5, solo
    los parquet). Para EURUSD, que aún no tiene data real descargada, Claude
    generó M5 SINTÉTICO solo para validar que la lógica del SCALPER corre — los
    números de un backtest sintético NO son predictivos, solo confirman que no
    crashea. Tunear SIEMPRE sobre data real.
    Bug encontrado y corregido: el backtester recalculaba indicadores por
    ventana (lentísimo) y `df_full.iloc[i+1:]` creaba slices gigantes (OOM).

DASHBOARD — MT5 EN SUBPROCESO
    Cuando se usa `scripts.serve`, el dashboard corre como subproceso de
    Streamlit independiente del orchestrator. Cada proceso tiene su propia
    conexión MT5. Mismas credenciales en .env, conexiones distintas — MT5
    permite múltiples sesiones Python contra el mismo terminal.

NOMENCLATURA
    "R" = riesgo por trade. PnL en R significa "múltiplos del riesgo arriesgado".
    Un trade que da +1R ganó lo mismo que se arriesgó. Es la unidad estándar
    para comparar trades de tamaños distintos.


################################################################################
#  §13  FRONT-END "CRIMSON BLACK"  (UI v4)  —  2026-05-20
################################################################################

PROPÓSITO
    Adaptación del dashboard a un nuevo sistema visual rojo/negro pensado para
    hosting público (demo del bot). TODA la funcionalidad previa se preservó —
    cambia exclusivamente la capa de presentación.

ALCANCE Y NO-ALCANCE
    Se modifica   :  dashboard/app.py (rewrite del CSS y look-and-feel),
                     .streamlit/config.toml (tema oscuro nativo de Streamlit).
    Se preserva   :  imports, settings, AuditStore, MT5Client, lógica de tabs,
                     filtros, callbacks (set_take_trades), backtest_symbol,
                     _format_summary, descargas .txt, st.dataframe, st.metric,
                     st.toggle, splash + sidebar + 5 tabs top-level + sub-tabs
                     de Datos. Mismos nombres de session_state keys.
    NO se toca    :  engine/, strategies/, risk/, executor/, signals/, audit/,
                     learning/, scripts/, config/symbols.yaml, config/runtime.yaml,
                     pyproject.toml.
    Backup        :  dashboard/app.py.bak  (versión anterior intacta por si se
                     quiere revertir con `mv app.py.bak app.py`).

SISTEMA DE DISEÑO — "CRIMSON BLACK"
    Paleta principal
      · Fondo base    : #08080a → #0f0f12 (gradiente vertical)
      · Halo radial   : rgba(239, 68, 68, 0.10) arriba-izquierda
      · Tarjetas      : rgba(20, 20, 26, 0.78) con blur(8px)
      · Borde tarjeta : rgba(220, 38, 38, 0.18) (hover sube a 0.55)
      · Rojo primario : #ef4444  (var --tri-red)
      · Rojo profundo : #b91c1c  (var --tri-red-deep)
      · Rojo glow     : rgba(239, 68, 68, 0.35)
      · Gradiente     : linear-gradient(135deg, #ef4444, #b91c1c)
      · Texto         : #f5f5f7 (base) / #a0a0aa (muted) / #6b6b76 (dim)
      · Pills semánticas mantenidas:
            green  → estados positivos / cuenta OK
            blue   → estados informativos (NEW, PLACED, FILLED)
            orange → atención (REJECTED_CONFLUENCE, modo forzado)
            red    → rechazos / cierres en SL / cuenta desconectada
            gray   → neutro
            accent → rojo (sustituye al cyan/violeta anterior)

    Componentes con tratamiento nuevo
      · Tabs           : pill activa con gradiente rojo + sombra glow.
      · Botones        : borde rojo translúcido; hover → gradiente + lift -1px.
      · Cards          : barra lateral roja de 3px; hover sube borde y eleva 2px.
      · KPI            : barra superior con gradiente; lift en hover.
      · Header         : gradiente radial rojo con borde glowing.
      · Splash         : marca TRIARCH con gradiente + text-shadow rojo difuso.
      · Sidebar        : fondo casi negro con halo rojo arriba, bloques
                         hover-able con borde que se enciende.
      · Scrollbar      : gradiente rojo profundo (solo navegadores webkit).
      · st.metric      : envuelta con borde rojo translúcido + hover.

    Tipografía
      · Inter / Segoe UI / system-ui (fallback). Letter-spacing sutil.
      · Pesos: 600-900 para títulos, 700 para labels uppercase.

CAMBIOS DE COPY / TEXTO
    Solo se actualizó la etiqueta de versión en el footer del sidebar:
        "v0.3 · UI v3"  →  "v0.4 · UI v4 · Crimson Black"
    El resto del texto en español queda idéntico (filtros, captions, mensajes
    de empty state, tooltips de los toggles, etc.).

CONFIG.TOML DE STREAMLIT  (.streamlit/config.toml)
    Nuevo archivo para que widgets nativos respeten la paleta sin depender
    solo del CSS injectado:
        [theme]
        base = "dark"
        primaryColor          = "#ef4444"
        backgroundColor       = "#0f0f12"
        secondaryBackgroundColor = "#14141a"
        textColor             = "#f5f5f7"
        font                  = "sans serif"

        [server]   headless = true,  runOnSave = false
        [browser]  gatherUsageStats = false

CÓMO LANZARLO  (sin cambios respecto a antes)
    Desarrollo directo del dashboard:
        streamlit run dashboard/app.py
    Combinado con loop principal (lo recomendado para hosting):
        python -m scripts.serve --tick 30 --port 8765
    El config.toml se aplica automáticamente al estar en la raíz del proyecto.

ROLLBACK
    Si la UI v4 da problemas en algún navegador o en mobile, basta con:
        cd dashboard
        mv app.py app.py.v4
        mv app.py.bak app.py
    Funcionalidad y datos intactos.

CHECKLIST DE VERIFICACIÓN  (correr antes de deploy)
    [ ] streamlit run dashboard/app.py  arranca sin errores.
    [ ] El splash aparece la primera vez y se cierra con "Entrar al bot →".
    [ ] Sidebar muestra brand, entorno, cuenta MT5 (o "desconectada"),
        kill switch, atajos y footer "v0.4 · UI v4 · Crimson Black".
    [ ] Tab Inicio renderiza 4 metrics de cuenta + KPI grid 24h + tarjetas
        por activo con dot/pill correctas.
    [ ] Tab Vivo & Control: el toggle "Trades reales en MT5" persiste cambios
        en config/runtime.yaml (verificar con set_take_trades).
    [ ] Tab Decisiones: filtros (activo, estrategia, estado, dirección, días)
        aplican y la descarga .txt baja un archivo nombrado correctamente.
    [ ] Tab Backtesting: el botón "▶ Correr backtest" llama a backtest_symbol
        sin tocar la lógica, y la descarga .txt invoca _format_summary.
    [ ] Tab Datos sub-tabs Signals/Evals/Stats muestran los st.dataframe ok.
    [ ] No quedaron referencias a la paleta cyan/violeta anterior
        (--tri-accent / --tri-accent-2) en el render final.

NOTA PARA FUTURAS ITERACIONES DE UI
    Si se quiere migrar a un front desacoplado (Next.js + FastAPI), las
    fuentes de datos son las que ya están en uso:
      · AuditStore (audit/store.py)   →  list_signals / list_evals
      · MT5Client (data_layer/...)    →  account_info / get_rates
      · config.runtime                →  get_take_trades / set_take_trades
      · scripts.backtest              →  backtest_symbol / _format_summary
    En ese caso, esta versión Streamlit puede coexistir como "panel interno"
    mientras la web pública la sirve un backend más liviano.


################################################################################
#  §14  FRONT-END ESTÁTICO PARA GITHUB PAGES  —  2026-05-20
################################################################################

MOTIVO
    GitHub Pages SOLO sirve archivos estáticos: no corre Python, no corre
    Streamlit, no puede conectarse a MT5. Para poder publicar el dashboard del
    bot en una URL pública (jpfrago.github.io/triarch o similar) se construyó
    una versión HTML/CSS/JS independiente que vive en /docs y consume datos
    exportados desde la lógica del bot en local.

    Esto convive con la versión Streamlit (UI v4) — NO la reemplaza.
    Streamlit sigue siendo la herramienta operativa en local (con MT5 vivo,
    toggles persistentes, run de backtest on-demand). La web estática es la
    capa de visualización pública / portfolio.

ARQUITECTURA NUEVA
    docs/                     ← carpeta raíz que GitHub Pages sirve
    ├── index.html            ← landing + 5 pestañas (Inicio, Vivo & Control,
    │                            Decisiones, Backtesting, Datos)
    ├── .nojekyll             ← evita procesamiento Jekyll
    ├── README.md             ← guía de uso/deploy (no se sirve como página)
    ├── assets/
    │   ├── css/styles.css    ← sistema de diseño "Crimson Black" completo,
    │   │                       independiente del CSS embebido en Streamlit
    │   └── js/app.js         ← vanilla JS (sin frameworks ni bundler)
    │                           · carga ./data/state.json (fallback sample.json)
    │                           · render de las 5 pestañas + sub-tabs
    │                           · filtros client-side (activo, estrategia,
    │                             estado, dirección, últimos N días)
    │                           · export .txt en cliente (Blob + download)
    │                           · curva de equity con Canvas (sin libs)
    └── data/
        ├── sample.json       ← datos sintéticos para que la web sea usable
        │                       sin un export real (3 activos, 10 signals,
        │                       backtest mock con equity curve)
        └── state.json        ← (opcional) snapshot real del bot
                                generado por scripts.export_web

    scripts/export_web.py    ← script nuevo (entrypoint Python)
                                Lee SQLite (AuditStore) + MT5 (opcional)
                                + cache de backtest (data_cache/backtest_last.json
                                si existe) → escribe docs/data/state.json.
                                Flags: --signals-limit, --evals-limit, --no-mt5, --out.

CONTRATO DEL state.json
    {
      "meta":      {generated_at, is_sample, signals_count, evals_count},
      "settings":  {env, default_mode, conf_min_signals, conf_min_families,
                    conf_min_score, kill},
      "account":   {login, server, name, currency, balance, equity, margin,
                    free_margin, leverage}  | null,
      "symbols":   { NAME: {broker_symbol, description, profile, timeframe,
                    mode, take_trades, take_trades_live, session_start,
                    session_end, strategies[], family, target_trades_wk} },
      "signals":   [ … filas crudas de la tabla signals … ],
      "evals":     [ … filas crudas de la tabla evals … ],
      "backtest_results": [ … resultados de scripts.backtest si los hay … ]
    }
    Cualquier cambio de schema requiere alinear scripts/export_web.py y
    docs/assets/js/app.js a la par.

DIFERENCIAS WEB vs STREAMLIT  (intencionales)
    · "Trades reales en MT5" → en la web es SOLO LECTURA (refleja el
       take_trades_live del último export). En Streamlit persiste a
       config/runtime.yaml. Esto es a propósito: la web es pública.
    · "Correr backtest" → en la web NO se corre, solo se muestran los
       resultados de la última corrida en local (data_cache/backtest_last.json).
       El botón estaba en Streamlit y allí seguirá.
    · La cuenta MT5 en la web es del momento del export, no live.

DEPLOY EN GITHUB PAGES
    1. Repo en GitHub  →  Settings  →  Pages.
    2. Source = "Deploy from a branch".
    3. Branch = main  ·  Folder = /docs.
    4. URL queda en `https://<user>.github.io/<repo>/`.
    El .nojekyll asegura que el directorio se sirve raw (sin procesar markdown).

FLUJO DE ACTUALIZACIÓN DE DATOS
    python -m scripts.fetch_history --years 1               (una vez al mes)
    python -m scripts.backtest --out data_cache/backtest_last.json
    python -m scripts.export_web
    git add docs/data/state.json
    git commit -m "chore(web): refresh snapshot"
    git push
    → GitHub Pages re-deploya solo en ~1 min.

PRIVACIDAD
    El export incluye número de cuenta, balance, equity y nombre del servidor
    MT5. ANTES de pushear a un repo público:
      · Considerar `--no-mt5` (omite el bloque "account"),
      · O mantener el repo privado y usar GitHub Pages privado (plan Pro+),
      · O editar export_web.py para enmascarar `login` y `balance`.
    Por defecto el script no enmascara nada (uso personal).

CHECKLIST DE VERIFICACIÓN  (correr antes del primer deploy)
    [ ] Abrir docs/index.html localmente con un server (no file://):
            python -m http.server -d docs 8080
        ó usar la extensión "Live Server" en VS Code.
        Verificar que el splash sale, las 5 tabs renderizan, los filtros
        de Decisiones funcionan, el download .txt baja un archivo y
        el banner de "datos de muestra" aparece (porque solo está sample.json).
    [ ] python -m scripts.export_web   debe escribir docs/data/state.json sin
        errores, incluso sin MT5 (--no-mt5). El banner debe desaparecer al
        existir state.json.
    [ ] Después de subir a GitHub:
            Settings → Pages → folder /docs → URL operativa.
    [ ] Probar la URL pública en mobile: layout colapsa a 1 columna, las
        tablas siguen scrolleables.

DECISIONES DE DISEÑO  (registro)
    · No se usó React/Vue/Next por: peso, complejidad, build step → GitHub
      Pages sirve mejor sin build. Vanilla JS + Canvas para gráficos es
      suficiente para 5 pestañas y filtros client-side.
    · No se usó Chart.js / D3 — equity curve dibujada en Canvas con gradient
      rojo en menos de 50 líneas. Mantiene la paleta sin dependencias.
    · Tipografía: Inter + JetBrains Mono desde Google Fonts (1 sola request).
    · La paleta y los componentes son 1:1 con el CSS embebido en app.py
      (Streamlit UI v4) → si se itera el design system, hay que sincronizar
      docs/assets/css/styles.css. (TODO menor: extraer ambos a un solo
      tokens.css cuando crezca.)

CÓMO ABRIRLO LOCAL  (sin git, sin push)
    cd "<repo>"
    python -m http.server -d docs 8080
    → http://localhost:8080/   abre la web exactamente como la verá GitHub Pages.

ROLLBACK
    Borrar /docs/index.html, /docs/assets/, /docs/data/ y /docs/.nojekyll.
    Borrar scripts/export_web.py.
    El proyecto vuelve al estado pre-§14. (Streamlit y §13 quedan intactos.)


────────────────────────────────────────────────────────────────────────
ADDENDUM §14.1 — Fix deploy: mover sitio a la RAÍZ del repo  (2026-05-20)
────────────────────────────────────────────────────────────────────────

PROBLEMA OBSERVADO
    Tras subir /docs a GitHub, la URL `https://frago10.github.io/Triarch/`
    mostraba el README.md del proyecto renderizado por Jekyll (tema por
    defecto) en vez del dashboard. Las funcionalidades parecían "perdidas",
    pero en realidad el HTML estático nunca se sirvió: GitHub Pages estaba
    configurado para servir desde la raíz del branch main, no desde /docs/.

DIAGNÓSTICO
    Por defecto GH Pages busca `index.html` en la carpeta configurada como
    Source (root). Si no lo encuentra, Jekyll procesa README.md y lo sirve
    como página de portada con el tema "Cayman" o el que esté por defecto.
    Eso es lo que se ve en el screenshot del usuario.

FIX APLICADO  (cambios estructurales)
    Se duplica el sitio estático a la raíz del repo (junto al README.md):
        /index.html                ← copia de docs/index.html
        /.nojekyll                 ← evita que Jekyll procese README.md
        /assets/css/styles.css     ← copia de docs/assets/css/styles.css
        /assets/js/app.js          ← copia de docs/assets/js/app.js
        /data/sample.json          ← copia de docs/data/sample.json
    /docs/ se MANTIENE intacto por dos razones:
        (a) Contiene architecture.md y setup-mt5-demo.md (docs técnicos).
        (b) Permite alternar la config de GH Pages a /docs si en el futuro
            se prefiere aislar el sitio del root del repo.

    scripts/export_web.py se actualizó para:
        · Default OUT_PATH = REPO_ROOT/data/state.json (raíz, no /docs)
        · Escribe simultáneamente un MIRROR en docs/data/state.json
        · Docstring actualizado con la nueva ubicación

POR QUÉ ESTA ES LA SOLUCIÓN ROBUSTA
    Esta configuración funciona con AMBOS settings posibles de GH Pages:
        · Source: root del branch main      → sirve /index.html
        · Source: /docs del branch main     → sirve /docs/index.html
    El usuario no necesita cambiar nada en Settings → Pages.
    El .nojekyll en la raíz garantiza que el README.md no se renderice
    nunca como home — siempre gana el index.html.

QUÉ HACER PARA APLICAR EL FIX EN GITHUB
    Desde la carpeta del repo, en local:
        git add index.html .nojekyll assets/ data/ scripts/export_web.py
        git add docs/                                # opcional, por consistencia
        git commit -m "fix(web): serve dashboard from repo root for GH Pages"
        git push
    GH Pages re-deploya en ~1 minuto.  Refrescar la URL con Ctrl+F5 (hard
    reload) para evitar caché.

VERIFICADO LOCAL  (smoke test 2026-05-20)
    python -m http.server -d <repo_root> 8766
        GET /                       → 200  (sirve index.html)
        GET /index.html             → 200
        GET /assets/css/styles.css  → 200
        GET /assets/js/app.js       → 200
        GET /data/sample.json       → 200
        GET /.nojekyll              → 200

NOTA SOBRE EL DOBLE-FOLDER
    Tener /index.html y /docs/index.html no es ideal (duplicación). Para
    desduplicar en el futuro, dos opciones:
      A) Eliminar la copia en /docs/ y dejar solo la raíz. Mantener
         /docs/{architecture.md, setup-mt5-demo.md, README.md} como docs.
      B) Borrar las copias del root, configurar GH Pages a "Folder: /docs"
         en Settings, y revertir export_web.py al DEFAULT_OUT anterior.
    Por ahora se mantienen ambos para que el deploy funcione "out of the box".


################################################################################
#  §15  POOL AMPLIADO DE ESTRATEGIAS + FILTROS TEMPORALES  (v0.5)  —  2026-05-20
################################################################################

OBJETIVO DE LA FASE
    Subir el volumen y la calidad de señales en los 3 activos siguiendo el
    trade-off elegido por el usuario:  "RR alto, WR moderado" — apuntar a
    45-50% WR manteniendo RR 1.5-2.2, PF objetivo 1.50-1.65.

    Cambios pedidos por el usuario:
      · NAS100   : tomar MÁS trades + subir win rate moderadamente.
      · XAUUSD   : mantener perfil quality (RR alto, WR ~41%, PF 1.51) pero
                   subir un poco el cap para apuntar a ~3 trades buenos/sem.
      · EURUSD   : el backtest no mostraba trades — investigar y arreglar.
      · Todos    : 6 estrategias mínimo por activo, pueden compartirse entre
                   activos. Cada strategy se acopla "donde mejor encaje".
      · UX       : filtros de rango temporal en el backtesting.

ARCHIVOS TOCADOS  (resumen del diff)
    engine/indicators.py          → + keltner(), donchian(), macd()
                                    + ema_20 y ema_200 en add_default_indicators
    strategies/pullback_trend.py  → NUEVO  (familia trend, ~30 líneas lógica)
    strategies/donchian_break.py  → NUEVO  (familia structural)
    strategies/keltner_break.py   → NUEVO  (familia trend)
    strategies/macd_cross.py      → NUEVO  (familia trend)
    strategies/rsi_reversal.py    → NUEVO  (familia mean)
    strategies/registry.py        → registra las 5 nuevas (total 10 strats)
    config/symbols.yaml           → 6 strats por activo, sesiones ampliadas,
                                    caps subidos en NAS100 y XAUUSD
    dashboard/app.py              → presets temporales (1M/3M/6M/YTD/1Y/2Y)
                                    sobre el backtest tab (Streamlit)
    index.html                    → filtros from/to + selector de activo +
                                    botones preset en tab Backtesting
    assets/js/app.js              → recomputeMetricsFromLog(): recalcula
                                    métricas client-side cuando se cambia el
                                    rango temporal sin re-correr el backtest
    docs/{index.html, assets, data}  → mirror de los cambios de raíz

POOL DE ESTRATEGIAS  (10 totales, antes 5)
    ──────────────────────  ─────────────  ────────────────────────────────────
    Estrategia              Familia        Concepto
    ──────────────────────  ─────────────  ────────────────────────────────────
    ORB                     opening        Breakout de los primeros 15m
    EMA_MOMENTUM            trend          Pullback EXACTO a EMA21 (estricto)
    PULLBACK_TREND   NEW    trend          Pullback amplio a EMA20 (más laxo)
    KELTNER_BREAK    NEW    trend          Breakout banda Keltner + RSI confirm
    MACD_CROSS       NEW    trend          Cruz MACD/signal con filtro tendencia
    SCALPER                 trend          Tendencia corta M5 (EURUSD-tuned)
    VWAP_MR                 mean           Reversión intra-sesión a VWAP
    BB_MR                   mean           Reversión Bollinger
    RSI_REVERSAL     NEW    mean           Martillo/shooting-star + RSI<30/>70
    DONCHIAN_BREAK   NEW    structural     Breakout del máx/mín 20 velas
    ──────────────────────  ─────────────  ────────────────────────────────────

DISTRIBUCIÓN POR ACTIVO  (6 strats c/u, mix por familia)
    NAS100  (M15, balanced, 13:30-19:00 UTC, max_trades_day 8)
      · ORB · EMA_MOMENTUM · PULLBACK_TREND · MACD_CROSS · DONCHIAN_BREAK · VWAP_MR
      Confluencia 2/2 (calidad mantenida). Mix opening + trend + structural + mean.

    XAUUSD  (M30, quality, 08:00-20:00 UTC, max_trades_day 3)
      · EMA_MOMENTUM · PULLBACK_TREND · KELTNER_BREAK · DONCHIAN_BREAK
        · MACD_CROSS · RSI_REVERSAL
      Confluencia 1/1 (EMA_MOMENTUM es el edge probado; las demás aportan
      diversidad pero filtran por el min_rr_ratio=2.2 del activo). El nuevo
      cap diario 3 (era 2) busca subir de 2.6/sem a ~3.5/sem.

    EURUSD  (M5, scalper, 06:30-17:00 UTC, max_trades_day 10)
      · SCALPER · BB_MR · PULLBACK_TREND · MACD_CROSS · KELTNER_BREAK · RSI_REVERSAL
      Confluencia 1/1 (en M5 esperar a 2 strats coincidiendo es irrealista).
      min_rr_ratio sigue en 0.8 para permitir RR scalper (~0.85).

BUG EURUSD — DIAGNÓSTICO Y FIX
    Síntoma reportado: el backtest no mostraba trades para EURUSD.

    Investigación (2026-05-20):
      · data_cache/history/EURUSD_M5.parquet SÍ existe (1.4 MB, ~75k velas).
      · El bug previo (M5 + copy_rates_range en una sola llamada → vacío)
        ya estaba arreglado en data_layer/mt5_client.get_rates desde
        §03 (2026-05-14): ahora descarga en chunks de ~5000 velas.
      · La causa real era la combinación:
          (a) Solo 2 strats activas (SCALPER + BB_MR), ambas muy restrictivas.
          (b) SCALPER tiene filtro de sesión interno + el backtester aplica
              filtro de sesión OTRA vez → doble filtrado innecesario.
              (Sin embargo, esto solo es ineficiencia, no bug — el filtro
              backtester usa el mismo session_utc.)
          (c) min_signals=1 + confluencia que elige una sola → si BB_MR
              dispara fuera de sesión, queda descartada por el orchestrator
              vela tras vela.

    Fix aplicado (no requiere código en engine):
      1. EURUSD ahora tiene 6 strats (en vez de 2) → más probabilidades de
         que al menos una dispare cada vela con setup válido.
      2. Sesión ampliada 06:30-17:00 UTC (en vez de 07:00-16:00) → más velas
         dentro de la ventana operativa.
      3. max_trades_per_day subido de 8 a 10.
      4. (Pendiente para usuario): si tras backtest sigue dando 0 trades en
         data real, correr `python -m scripts.fetch_history --symbol EURUSD
         --years 2` para asegurar histórico completo, y revisar broker_symbol
         contra `python -m scripts.diagnose_mt5`.

FILTROS TEMPORALES — UX
    Streamlit  (dashboard/app.py tab Backtesting):
      · Mantiene los st.date_input "Desde / Hasta" originales.
      · NUEVO: fila de 6 botones preset bajo los date inputs:
            1M · 3M · 6M · YTD · 1Y · 2Y
        Cada uno setea session_state["bt_from_val"] y bt_to_val y st.rerun().
        El usuario puede después ajustar manualmente con los date inputs.

    Estático  (index.html + assets/js/app.js tab Backtesting):
      · Nueva filter-bar arriba de los resultados:
            Desde (date input)  ·  Hasta (date input)  ·  Activo (select)  ·  Reset
      · Fila preset con 6 botones (mismos rangos que Streamlit).
      · NUEVA función JS recomputeMetricsFromLog(original, fromTs, toTs):
          - Filtra trade_log por rango.
          - Recalcula WR, PF, expectancy, Sharpe, Sortino, SQN, max_DD,
            avg_win/loss, streaks y trades_per_week SOBRE el subset.
          - Reconstruye equity_curve acumulando pnl_r del subset.
          - by_strategy se recalcula desde el subset.
      · Esto NO re-corre el backtest (imposible en GH Pages estático) sino
        que recalcula desde el trade_log ya exportado por scripts.export_web.
      · La curva de equity y la tabla de trade log respetan el rango.

REALIDAD MATEMÁTICA DEL TRADE-OFF WR/RR  (registrar — el usuario aceptó)
    El usuario pidió originalmente "60% WR + RR 1.5+ para todos". Se le
    explicó que es matemáticamente imposible: WR × RR_promedio determina
    el PF, y un sistema 60%/RR1.5 tendría PF 2.25 (territorio de los mejores
    sistemas legendarios). Aceptó "RR alto, WR moderado" — apuntar a
    45-50% WR manteniendo RR 1.5-2.2 → PFs 1.50-1.65 (sólido, alcanzable).

    Targets actuales (a verificar con backtest):
      · NAS100  M15  → ~8-12 trades/sem · WR 45-50% · PF 1.30-1.50
      · XAUUSD  M30  → ~3-4 trades/sem  · WR 40-45% · PF 1.50-1.70
      · EURUSD  M5   → ~25-50 trades/sem · WR 50-55% · PF 1.10-1.25
    Si el primer backtest no se acerca, tunear parámetros de strategies
    individuales (no relajar las reglas inquebrantables del risk manager).

CHECKLIST DE VERIFICACIÓN  (correr antes de declarar v0.5 lista)
    [ ] python -m scripts.fetch_history --years 1   (refresca histórico)
    [ ] python -m scripts.backtest                  (los 3 activos)
        ─ NAS100 debe mostrar trades (antes ya mostraba)
        ─ XAUUSD debe seguir rentable con EMA_MOMENTUM (PF >= 1.4)
        ─ EURUSD AHORA debe mostrar trades (el fix de §15)
    [ ] streamlit run dashboard/app.py  → tab Backtesting con presets
    [ ] http://localhost:8080 contra el index.html de raíz → filtros temporales
    [ ] Si todo OK: python -m scripts.export_web  +  git push

ROLLBACK
    Si v0.5 degrada el PF de XAUUSD:
      git checkout HEAD~1 config/symbols.yaml strategies/registry.py
    Las 5 strategies nuevas pueden borrarse sin afectar al resto (nada
    importa de ellas). engine/indicators.py es retrocompatible
    (los indicadores nuevos son aditivos, no rompen las strategies viejas).


################################################################################
#  §16  BACKTESTER EN JAVASCRIPT  (v0.6 — corre en browser)  —  2026-05-20
################################################################################

PROBLEMA QUE RESUELVE
    En §15 los "filtros temporales" en la web solo recortaban un trade_log
    ya exportado desde Python. El usuario dijo claramente: "quiero poder
    correr el backtesting desde la pagina ... ahorita mismo no actualiza
    lo de eurusd ni lo otro". Necesitaba ejecutar EL MOTOR, no filtrar un
    resultado precomputado. Y GitHub Pages no puede correr Python.

SOLUCIÓN
    Portar el motor de backtest a JavaScript puro, con datos pre-computados
    (OHLC + indicadores ya calculados) servidos como JSON estático.

    Flujo nuevo:
      LOCAL (1 vez por descarga de histórico):
          python -m scripts.export_ohlc       → data/ohlc/{SYM}.json
          python -m scripts.refresh_web       → todo de un golpe
          git add data/ docs/ && git push
      BROWSER (cada vez que el usuario abra la página):
          fetch data/ohlc/manifest.json       → lista de activos disponibles
          fetch data/ohlc/{SYM}.json          → OHLC + indicadores precalc.
          window.TriarchBT.runBacktest({...}) → corre estrategias en JS
          render resultados                    → misma UI rojo/negro existente

ARCHIVOS NUEVOS
    scripts/export_ohlc.py         — Exporta OHLC + indicadores precalculados.
                                     Una entrada por vela, formato compacto
                                     (~500KB por símbolo para ~2k velas).
    scripts/refresh_web.py         — Pipeline único:
                                       fetch_history (--fetch opcional)
                                       + backtest
                                       + export_ohlc
                                       + export_web
                                     Para que el usuario solo corra UN comando.
    assets/js/bt-engine.js         — Motor JS: implementa 10 estrategias
                                     idénticas a las Python, applyConfluence,
                                     resolveTrade, computeMetrics. ~600 líneas
                                     puramente vanilla, no requiere libs.
    data/ohlc/{NAS100,XAUUSD,EURUSD}.json
                                   — OHLC sintético de muestra (4k velas c/u
                                     con drift sinusoidal para que las strats
                                     de tendencia tengan algo que explotar).
                                     SE REEMPLAZA al correr export_ohlc en
                                     local con MT5 real.
    data/ohlc/manifest.json        — Lista de símbolos disponibles + fecha
                                     de generación. La web lo lee para
                                     llenar el selector.

ARCHIVOS MODIFICADOS
    index.html                     — Nuevo botón "▶ Correr backtest"
                                     + barra de status que muestra el
                                     manifest de OHLC disponible.
    assets/js/app.js               — Función renderBacktest() ahora pide al
                                     motor JS correr el backtest con los
                                     filtros del usuario y el OHLC cargado.
                                     Cache OHLC_CACHE para no re-descargar.

CONTRATO DEL ENGINE JS
    En global: window.TriarchBT.runBacktest(options) → result
      options = {
        ohlc:         payload de data/ohlc/{SYM}.json,
        strategies:   ['EMA_MOMENTUM', 'PULLBACK_TREND', ...],
        confluence:   { min_signals, min_families, min_combined_score },
        minRR:        cfg.risk.min_rr_ratio del activo,
        maxTradesDay: cfg.risk.max_trades_per_day del activo,
        fromTs:       epoch ms (UTC) o null,
        toTs:         epoch ms (UTC) o null,
      }
      result mantiene el MISMO shape que backtest_symbol() de Python:
        symbol, timeframe, trades, win_rate, profit_factor, expectancy_r,
        sharpe_ratio, sortino_ratio, sqn, max_drawdown_r, avg_win_r,
        avg_loss_r, longest_win_streak, longest_loss_streak,
        trades_per_week_avg, by_strategy, trade_log, equity_curve.
      Esto permite que drawBacktestResults() (que ya existía) renderice los
      resultados del JS sin tocar nada.

ESTRATEGIAS PORTADAS  (paridad 1:1 con Python registry)
    EMA_MOMENTUM, PULLBACK_TREND, KELTNER_BREAK, MACD_CROSS, SCALPER (trend)
    VWAP_MR, BB_MR, RSI_REVERSAL                                     (mean)
    DONCHIAN_BREAK                                                   (structural)
    ORB                                                              (opening)
    Los parámetros DEFAULT son idénticos a Python; los rr_target respetan
    el min_rr del activo (max(self.rr, cfg.minRR)).

INDICADORES — NO SE RECALCULAN EN JS
    Decisión de diseño: scripts/export_ohlc.py calcula EMA9/20/21/50/200,
    ATR14, RSI14, Bollinger, Keltner, Donchian, MACD/signal/hist, VWAP,
    Opening Range, y los EMBEBE en cada fila del JSON. El JS solo aplica
    reglas if/else. Ventajas:
      · Engine JS más simple (~600 líneas en vez de ~1500).
      · Garantía de paridad: los números que ve la web son IDÉNTICOS a los
        que produciría el backtest de Python sobre el mismo histórico.
      · ~10× más rápido cargar números que recalcular.
    Coste: cada JSON pesa ~500KB para 2k velas (~3-4MB para 1 año de M5).
    Para cachear: fetch usa "force-cache" para no re-descargar entre runs.

RENDIMIENTO  (medido en local con node)
    Engine JS sobre 2000 velas + 6 strategies con confluencia:
      NAS100  M15: 40 trades  en ~20 ms
      XAUUSD  M30: 122 trades en ~14 ms
      EURUSD  M5:  120 trades en ~8 ms
    Para 75k velas (1 año M5) el engine debería tardar ~300-500ms en
    browser moderno → totalmente usable. Si llegara a sentirse lento,
    mover el engine a un Web Worker es trivial (window → self).

DATA SINTÉTICA POR DEFECTO
    Los data/ohlc/*.json incluidos en el repo son SINTÉTICOS (random walk
    + ciclo sinusoidal, generados con seed 42). El campo `_synthetic: true`
    los marca. Los números que produzca el backtest sobre ellos NO SON
    PREDICTIVOS — el motor solo está demostrando que funciona.

    Para data real:
        python -m scripts.fetch_history --years 2
        python -m scripts.export_ohlc
        git push
    A partir de ese push, los archivos data/ohlc/*.json contendrán velas
    reales descargadas vía MT5 y el flag _synthetic desaparece.

VERIFICADO LOCAL
    [OK] node parsea bt-engine.js sin errores.
    [OK] node parsea app.js sin errores.
    [OK] HTTP smoke test: index.html, bt-engine.js, app.js, manifest.json,
         data/ohlc/{NAS100,XAUUSD,EURUSD}.json todos sirven 200.
    [OK] End-to-end: window.TriarchBT.runBacktest() devuelve trades válidos
         con confluencia 2/2 aplicada (NAS100) y 1/1 (XAU/EUR).
    [OK] Filtros temporales: dividir el rango por la mitad da trades
         primer-mitad + segunda-mitad ≈ total. Comprueba que respeta from/to.

CHECKLIST DEL USUARIO  (correr en local antes de declarar v0.6 listo)
    1. python -m scripts.fetch_history --years 2
    2. python -m scripts.refresh_web
       (esto corre backtest snapshot + export_ohlc + export_web)
    3. git add data/ docs/
       git commit -m "data: OHLC real + state refresh"
       git push
    4. Abrir https://frago10.github.io/Triarch/ con Ctrl+F5
       → tab Backtesting → seleccionar Activo, fechas, "Correr backtest"
       → debería mostrar trades reales del activo elegido.

ROLLBACK
    Si el motor JS da resultados que no cuadran con Python:
      a) Borrar assets/js/bt-engine.js y la sección "Backtest interactivo"
         de app.js. La web vuelve al modo "viewer" de §15.
      b) NUNCA borrar export_ohlc.py — es útil incluso sin engine JS para
         debugging del Python.


################################################################################
#  §17  CALIBRACIÓN v0.7 SOBRE DATA MT5 REAL  —  2026-05-26
################################################################################

CONTEXTO
    Usuario reportó "según los parámetros cuando se corre el back testing no se
    registra ningún trade". Diagnóstico: la data sintética de §16 solo cubría
    15-93 días según el símbolo, así que rangos como 6M o 1Y daban 0 trades.
    Decisión: ejecutar el backtest Python con data MT5 REAL (1 año en cada activo)
    y calibrar las strats según resultados objetivos.

DATA REAL DISPONIBLE
    data_cache/history/NAS100_M15.parquet   6 475 velas  (2025-05-12 → 2026-05-11)
    data_cache/history/XAUUSD_M30.parquet  11 718 velas  (2025-05-12 → 2026-05-12)
    data_cache/history/EURUSD_M5.parquet   74 149 velas  (2025-05-16 → 2026-05-15)
    .venv/Scripts/python.exe tiene pandas 3.0 + pyarrow 24, así que se pudo
    correr backtest desde la sesión de calibración sin tocar local.

RESULTADO PRIMERA PASADA (config previa a calibración, v0.6)
    NAS100  243 trades  WR 42.4%  PF 1.16  E +0.088R  Sharpe 1.10  DD 20.5R
        ORB pierde plata: 141 trades, E -0.05R, total -6.75R
    XAUUSD  707 trades  WR 34.8%  PF 1.16  E +0.107R  Sharpe 1.12  DD 27.4R
        Volumen excesivo (13.3/sem). User quería 3 buenos/sem.
        PULLBACK_TREND -8.8R, RSI_REVERSAL -3.8R.
    EURUSD  2590 trades WR 38.9%  PF 1.01  E +0.005R  Sharpe 0.07  DD 158.3R
        Casi breakeven. BB_MR -25.8R, KELTNER_BREAK -22.9R destruían.
        PULLBACK_TREND era lo único positivo (+36R).

DECISIONES POR ACTIVO
    NAS100
        QUITAR ORB (-6.75R en 141 trades, diluía sin aportar).
        AÑADIR KELTNER_BREAK.
        Cap diario 8 → 6 (estaba subutilizado, daba 4.86/sem ya).

    XAUUSD
        Cap diario 3 → 1 (5 trades/sem MAX, con conf 2/2 sale ~3-4/sem).
        SUBIR confluence de 1/1 a 2/2 → filtro de calidad fuerte.
        QUITAR EMA_MOMENTUM en una iteración intermedia que perdió plata
            con conf 2/2 (-11.6R) — el "edge probado" deja de serlo en
            modo confluencia exigente porque cambia qué se elige.
        QUITAR RSI_REVERSAL (-3.8R).
        AÑADIR ORB (sorprendentemente +45R con conf 2/2 — pasó de peor a top).
        AÑADIR VWAP_MR para tener familia "mean" disponible para conf 2/2.

    EURUSD
        QUITAR BB_MR (-25.8R), KELTNER_BREAK (-22.9R), DONCHIAN_BREAK (en M5),
            RSI_REVERSAL (lo intenté en v0.7 iteración intermedia, mejor sin).
        AÑADIR EMA_MOMENTUM (complementa a PULLBACK_TREND).
        AÑADIR VWAP_MR (con conf 2/2 se vuelve dominante — efecto inverso al
            que perdía sola).
        SUBIR confluence a 2/2 (era 1/1) — el principal motor del PF 1.56.
        Mantener min_rr_ratio en 0.8 (subirlo rompió las strats scalper).
        Cap diario 10 → 8.

BUG DEL MOTOR JS — VWAP_MR mal portado
    El JS de VWAP_MR usaba lógica diferente al Python:
      · Python: dispara con devAtr >= 1.5 ATR del VWAP + filtro anti-trend
        (no opera si EMAs muy separadas).
      · JS (bug):  disparaba con devAtr < -1.0, exigía RSI extremo, y filtraba
        contra-tendencia. Resultado: 14 disparos en 1 año vs 110 de Python.
    Esto explicaba por qué EURUSD daba 0 trades en JS (con conf 2/2 sin VWAP_MR
    no se llegaba a 2 familias). FIX en assets/js/bt-engine.js — paridad 1:1
    con strategies/vwap_mr.py.

PARIDAD POST-FIX  (Python vs JS sobre la misma data 1 año)
    Symbol   Python                JS                   Match
    NAS100   209 tr, PF 1.35       245 tr, PF 1.36      ✓ similar
    XAUUSD   209 tr, PF 1.27       236 tr, PF 1.26      ✓ similar
    EURUSD   171 tr, PF 1.56       173 tr, PF 1.52      ✓ casi idéntico
    Diferencias chicas vienen de pequeños matices en how each implementation
    resuelve confluence ties y rounding en floats. Es aceptable para una vista
    interactiva — la web puede correr backtests con cualquier rango y los
    números serán fieles a Python ±5%.

RESULTADOS FINALES POR RANGO  (backtest Python sobre data real)
    NAS100 M15 (6 strats, conf 2/2, cap 6/día, RR≥1.5):
        1Y: 209 tr  WR 42.6%  PF 1.35  E +0.20R  Sharpe 2.24  DD 17R   4.3/sem
        6M: 113 tr  WR 46.9%  PF 1.66  E +0.35R  Sharpe 3.82  DD 11R   4.7/sem
        4M:  68 tr  WR 44.1%  PF 1.46  E +0.25R  Sharpe 2.83  DD 11R   4.2/sem
        3M:  51 tr  WR 41.2%  PF 1.27  E +0.16R  Sharpe 1.79  DD 11R   4.2/sem
        1M:   9 tr  WR 33.3%  PF 0.93  (muestra muy chica)

    XAUUSD M30 (6 strats, conf 2/2, cap 1/día, RR≥2.2):
        1Y: 209 tr  WR 39.2%  PF 1.29  E +0.17R  Sharpe 1.85  DD 13.9R  4.0/sem ⭐
        6M: 103 tr  WR 36.9%  PF 1.19  E +0.12R  Sharpe 1.27  DD 13.5R  4.0/sem
        4M:  66 tr  WR 43.9%  PF 1.59  E +0.31R  Sharpe 3.35  DD 5R     3.9/sem
        3M:  49 tr  WR 42.9%  PF 1.55  E +0.30R  Sharpe 3.17  DD 5R     3.8/sem
        1M:  14 tr  WR 50.0%  PF 1.83  E +0.36R  Sharpe 4.16  DD 3R     3.5/sem ⭐

    EURUSD M5 (6 strats, conf 2/2, cap 8/día, RR≥0.8):
        1Y: 169 tr  WR 37.3%  PF 1.56  E +0.35R  Sharpe 2.53  DD 21R    3.8/sem
        6M:  83 tr  WR 38.6%  PF 1.55  E +0.34R  Sharpe 2.76  DD 13R    4.0/sem
        4M:  60 tr  WR 38.3%  PF 1.61  E +0.38R  Sharpe 2.88  DD 12R    4.3/sem
        3M:  48 tr  WR 39.6%  PF 1.83  E +0.50R  Sharpe 3.60  DD 12R    4.8/sem
        1M:   6 tr  (muestra muy chica)

INTERPRETACIÓN
    · Sistema RENTABLE en los 3 activos en TODOS los rangos ≥3M.
    · NAS100 y EURUSD están mejorando en los últimos 3-6 meses → bien
      sincronizados con el régimen de mercado actual.
    · XAUUSD genera 4.0 trades/sem (target del usuario: ~3 buenos/sem) ⭐.
    · WR 37-50% con RR 1.5-2.2 → PFs 1.27-1.83 — exactamente lo que
      anticipaba la matemática de "RR alto, WR moderado" elegida por el user.
    · El 1M de NAS100/EURUSD sale chico porque la muestra (6-11 trades) no
      es estadísticamente significativa. Esto es esperado, no es un bug.

ARCHIVOS AFECTADOS  (v0.7)
    config/symbols.yaml             — 3 activos re-calibrados con strats nuevas,
                                       caps ajustados, confluence 2/2 universal.
    assets/js/bt-engine.js          — VWAP_MR re-implementado con paridad 1:1.
    docs/assets/js/bt-engine.js     — mirror.
    data/ohlc/{NAS100,XAUUSD,EURUSD}.json + docs/data/ohlc/*
                                    — OHLC REAL exportado desde MT5 parquet:
                                      NAS100: 1.4 MB  (6475 velas)
                                      XAUUSD: 3.0 MB  (11718 velas)
                                      EURUSD: 16.3 MB (74149 velas)
                                      GH Pages sirve con gzip → ~4-5 MB en wire.
    data/ohlc/manifest.json         — generado_at + lista de símbolos.
    contexto.txt                    — este apartado §17.

CÓMO REPRODUCIR LA CALIBRACIÓN  (cuando llegue nuevo histórico)
    1. python -m scripts.fetch_history --years 1
    2. .venv/Scripts/python.exe -m scripts.backtest    (ver resultados Python)
    3. Identificar strats que pierden plata por activo, ajustar symbols.yaml.
    4. python -m scripts.export_ohlc                   (genera OHLC para JS)
    5. node test rápido del motor JS para verificar paridad.
    6. git push → GH Pages re-deploya.

TAMAÑO DE LOS JSON (consideración a futuro)
    EURUSD M5 = 16.3 MB en disco, ~4-5 MB con gzip. Si llega a ser molesto:
      A) Reducir precisión decimal de 6 a 5 (FX no necesita más).
      B) Strip velas fuera de sesión (06:30-17:00 UTC = solo 44% del día).
         Cuidado: cambia el shape del df y rompe las strats que usan prev_bar.
         Habría que añadir "session_only" flag en export_ohlc.
      C) Recortar a 6 meses (los presets 1Y dejarían de funcionar).

PRÓXIMOS PASOS POSIBLES
    · Probar el motor JS en GH Pages real con la data nueva (Ctrl+F5).
    · Si los números se sienten bien, abrir el toggle take_trades del activo
      con mejor backtest y poner el bot en SIGNAL_ONLY en producción (paper).
    · Re-correr este pipeline cada mes para reajustar si el régimen cambia.


################################################################################
#  FIN — actualizar §03, §10 y §11 al final de cada sesión
################################################################################
